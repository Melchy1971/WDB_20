// Platzhalter — PST-Import noch nicht implementiert.
export function PstImportPage() {
  return (
    <div className="page">
      <h1>PST-Import</h1>
      <p className="hint">PST-Import noch nicht verfügbar — in Planung.</p>
    </div>
  );
}
