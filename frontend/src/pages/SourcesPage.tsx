// TODO: GET /sources und POST /sources sind im Backend noch nicht implementiert.
// SourcesPage zeigt vorerst einen Platzhalter.
export function SourcesPage() {
  return (
    <div className="page">
      <h1>Quellen</h1>
      <p className="hint">
        Quellenverwaltung noch nicht verfügbar — Backend-Endpoint fehlt.
      </p>
    </div>
  );
}
